start;

int timestamp;
float temp;
float humidity;
float light;

read_from_sensor(temp_sensor,temp);
read_from_sensor(humidity_sensor,humidity);
read_from_sensor(light_sensor,light);
time_from_timer(timestamp);

string URL1;
print("URL to connect to: ");
input(URL1);
Connection connection1 = new Connection();
connection1.connect(URL1);

string input1;
connection1.input_from_connection(input1);

print("input from connection"); print(input1);
connection1.output_to_connection("mkdir newFolder");

status status1;
connection1.getStatus(status1);
if(status1 == connected){
    print("connected successfully");
}else{
    print("not connected");
}

protocol protocol1;
connection1.getProtocol(protocol1);
if(protocol1 == http){
    print("the protocol is http");
}else{
    print("the protocol is not http");
}

string url;
connection1.getURL(url);

#Arithmetic Operations/assignments/declerations
print((12 + 2 * 4) / (7));
int number;
number=9;
int num=1;
int numneg=-1;
float flt1=3.8;
float fltneg=-9.8;

#Declerations
string str1="arda";
string str3="sel";
char chr1='a';

#logic declerations
bool bool1=true;
bool bool2=on;
bool bool3=(bool1&bool2)


#for the given b do not print
int b=10;
for(int i=0; i<12, i=i + 1){
    if(i==b){
        continue;
    }
    print(i);
}


#While check
while(b!=0){
    b=b - 1;
    if(b==10){
        break;
    }
}
print(b);

#Function definition that checks the given integer if it is odd or even
func OddEven(int integer, string result){
    while(integer>1)
        if(((integer!=1)|(integer!=0))&(integer>0)){
            result = "it is an odd number";
        }else{
            result = "it is an even number";
        }
        integer=integer / 2;
    return;
}
#Function call
string result;
int a=5;
OddEven(a, result);
print(result);

#take input
int in = input()
end;